const config = {
  trailingComma: "es5",
  tabWidth: 2,
  semi: true,
  singleQuote: false,
  quoteProps: "as-needed",
  bracketSpacing: true,
  arrowParens: "avoid",
  parser: "typescript",
  printWidth: 100,
  plugins: ["prettier-plugin-tailwindcss"],
};

export default config;
